export type Message = {
  username: string;
  text: string;
  date: Date;
};

export type VideoDataType = {
  description: string;
  subtitle: string;
  sources: string;
  thumb: string;
  title: string;
};
